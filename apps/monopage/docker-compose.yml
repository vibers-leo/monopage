services:
  monopage-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monopage-app
    ports:
      - "4111:3000"
    restart: unless-stopped
    environment:
      - BACKEND_URL=http://172.17.0.1:4110
      - RAILS_API_URL=http://172.17.0.1:4110
      - ZEROCLAW_URL=http://172.17.0.1:42618
      - ZEROCLAW_TOKEN=zc_de636b3d09ca4ad2e186d4813561321f45a0588e42bb468820b5f56815e90bef
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=vibers.leo@gmail.com
      - SMTP_PASSWORD=ttiyfegllmwwyolt
      - SMTP_FROM=Monopage <vibers.leo@gmail.com>
    networks:
      - npm_default
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

networks:
  npm_default:
    external: true
